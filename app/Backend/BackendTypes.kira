struct BackendShaderHandle {
    var id: U32
}

struct BackendPipelineHandle {
    var id: U32
}

struct BackendBufferHandle {
    var id: U32
}

struct BackendTextureHandle {
    var id: U32
}

struct BackendSamplerHandle {
    var id: U32
}

struct BackendBindGroupHandle {
    var id: U32
}

struct BackendUniformHandle {
    var id: U32
}
