[package]
name = "triangle"
version = "0.1.0"
kind = "app"
kira = "0.1.0"
native_libraries = ["../../NativeLibs/Sokol.toml"]

[defaults]
execution_mode = "hybrid"
build_target = "host"

[dependencies]
KiraGraphics = { path = "../.." }
